!: Mathx program to check if given no is odd or even.

create variable num of type int.
print "Enter a no: ".
input to @num.

if (num mod 2 equal to 0) {
    print "%d is even", num.
} else {
    print "%d is odd", num.
}
