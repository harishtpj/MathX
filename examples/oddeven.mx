!: Mathx program to check if given no is odd or even.

main-para:-

create variable @num of type Integer.
print "Enter a no: ".
input to @num.

if num mod 2 equal to 0 then do
    print "%d is even", @num.
else do
    print "%d is odd", @num.
end
